# Direction B — reference material

Spec: [`../../UI_REDESIGN.md`](../../UI_REDESIGN.md)

- `preview/*.html` — six static screens, self-contained. Open in a browser, or
  read the markup for exact colours, sizes and spacing. These are the source of
  truth; the PNGs are for glancing at.
- `render/*.png` — the same screens as images.

Screens: overview, risks, findings, assets, attack-paths, remediation.

Not designed yet: light mode, widths below `lg`, and the remaining routes
(changes, reports, compliance, scans, rules, cloud).
